import React from "react";
import styles from "./Timer.module.css";

const Timer = () => {
  return <div>
  <div>
      <header>Working</header>
  </div>
  <div>
      25:00
  </div>
  <div>
      <button>Start</button>
      <button>Stop</button>
  </div>
  </div>;
};

export default Timer;
